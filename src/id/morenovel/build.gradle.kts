plugins {
    id("yomikku.extension")
}

yomikku {
    name = "Vanovel"
    className = ".Vanovel"
    versionCode = 1
    theme("madara")
    source(name = "Vanovel", lang = "id", baseUrl = "https://vanovel.com")
}
