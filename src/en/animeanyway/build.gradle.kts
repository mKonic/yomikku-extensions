plugins {
    id("yomikku.extension")
}

yomikku {
    name = "Anime Anyway"
    className = ".AnimeAnyway"
    versionCode = 1
    source(name = "Anime Anyway", lang = "en", baseUrl = "https://animeanyway.com")
}
