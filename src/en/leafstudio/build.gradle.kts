plugins {
    id("yomikku.extension")
}

yomikku {
    name = "LeafStudio"
    className = ".LeafStudio"
    versionCode = 1
    source(name = "LeafStudio", lang = "en", baseUrl = "https://leafstudio.site")
}
