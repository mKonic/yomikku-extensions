plugins {
    id("yomikku.extension")
}

yomikku {
    name = "KnoxT"
    className = ".KnoxT"
    versionCode = 1
    theme("lightnovelwp")
    source(name = "KnoxT", lang = "en", baseUrl = "https://knoxt.space")
}
