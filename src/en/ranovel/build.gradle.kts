plugins {
    id("yomikku.extension")
}

yomikku {
    name = "Ranovel"
    className = ".Ranovel"
    versionCode = 1
    theme("madara")
    source(name = "Ranovel", lang = "en", baseUrl = "https://ranovel.com")
}
