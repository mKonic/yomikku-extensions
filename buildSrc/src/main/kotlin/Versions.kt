object Versions {
    /** The source API version extensions are built against; the app loads the ones it lists as supported. */
    const val LIB_VERSION = "1.0"

    const val MIN_SDK = 26
    const val COMPILE_SDK = 37
}
